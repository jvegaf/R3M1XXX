# Tech Stack (detectado del repo)

- PlatformIO
  - Entorno: `[env:esp32-c3-devkitm-1]`
  - Plataforma: `espressif32` (versión no fijada)
  - Framework: `arduino`
  - Board: `esp32-c3-devkitm-1`
- Lenguaje: C/C++ (estándar no explicitado)
- Dependencias: No declaradas en `lib_deps`

Notas
- Sin versiones bloqueadas: evita depender de APIs recientes no ampliamente soportadas hasta fijar versiones.
- Añade aquí cualquier versión concreta cuando se fije (por ejemplo, `platform = espressif32@<versión>` o `build_flags = -std=gnu++17`).